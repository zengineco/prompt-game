# Copy to .env.local and fill in. (Vite only exposes vars prefixed with VITE_.)
# Your Discord Application ID (Dev Portal → General Information → Application ID)
VITE_DISCORD_CLIENT_ID=your_application_id_here
